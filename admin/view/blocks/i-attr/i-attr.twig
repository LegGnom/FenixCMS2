{#
	Формирование атрибутов для тега
#}
{% macro iAttr(options) %}
{% spaceless %}
	{% for key in options|keys %}
		{% if options[key] %}
			{{ key }}="{{ options[key] is iterable ? options[key]|json_encode : options[key] }}"
		{% endif %}
	{% endfor %}
{% endspaceless %}
{% endmacro %}